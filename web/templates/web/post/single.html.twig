{% extends 'base.html.twig' %}

{% block title %}{{ post.title }}{% endblock %}

{% block body %}
    <h1>{{ post.title }}</h1>
    {{ easy_gutenberg(post.content) }}

    {{ easy_gutenberg_assets() }}
{% endblock %}